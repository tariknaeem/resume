name: Deploy Resume

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            mkdir -p ~/apps/resume
            rm -rf /tmp/resume-deploy
            git clone https://github.com/tariknaeem/resume.git /tmp/resume-deploy
            cp /tmp/resume-deploy/*.html ~/apps/resume/
            cp /tmp/resume-deploy/*.jpg ~/apps/resume/ 2>/dev/null || true
            cp /tmp/resume-deploy/*.png ~/apps/resume/ 2>/dev/null || true
            cp /tmp/resume-deploy/Dockerfile ~/apps/resume/
            cp /tmp/resume-deploy/docker-compose.yml ~/apps/resume/
            cd ~/apps/resume && docker compose up -d --build
            rm -rf /tmp/resume-deploy
